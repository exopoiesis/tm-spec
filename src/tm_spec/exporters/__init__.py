"""Exporters from TM-Spec to external archive formats.

Currently shipped:
    - ``tm_spec.exporters.nomad`` — NOMAD upload bundle (ZIP).

Planned (see ``docs/design-decisions.md`` Q-TMSPEC-3 / Q-TMSPEC-4):
    - QCSchema interop (MolSSI ecosystem).
    - AiiDA archive (full provenance graph).
"""
